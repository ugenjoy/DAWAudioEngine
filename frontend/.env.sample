# Auto-connect to backend WebSocket without showing connection page
# Uses window.location.host to connect (same origin, proxied by Nginx)
# Set to "true" for server/kiosk deployments
VITE_WS_AUTO_CONNECT=false
